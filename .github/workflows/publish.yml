name: Publish to GitHub Packages

on:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  publish:
    runs-on: windows-latest
    permissions:
      contents: read
      packages: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'
    
    - name: Restore dependencies
      run: dotnet restore encodingChange\encodingChange.csproj
    
    - name: Build and pack
      run: |
        dotnet build encodingChange\encodingChange.csproj --configuration Release --no-restore
        dotnet pack encodingChange\encodingChange.csproj --configuration Release --no-build -p:PackageVersion=1.0.0 --output nupkgs
    
    - name: Publish to GitHub Packages
      run: dotnet nuget push "nupkgs/*.nupkg" --source "https://nuget.pkg.github.com/frensqq/index.json" --api-key ${{ secrets.GITHUB_TOKEN }} --skip-duplicate